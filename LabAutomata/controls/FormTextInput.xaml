<UserControl x:Class="LabAutomata.controls.FormTextInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="FormInput"
             d:DesignHeight="450" d:DesignWidth="800">
    <TextBox Text="{Binding ElementName=FormInput, Path=Input}"
             HorizontalContentAlignment="Stretch"
             MinLines="{Binding ElementName=FormInput, Path=MinimumLines}"
             MaxLines="{Binding ElementName=FormInput, Path=MaximumLines}"
             Margin="5"
             Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Width}"
             Style="{StaticResource TxtBoxDefault}"
             VerticalAlignment="Center"
             FontSize="24"/>
</UserControl>
